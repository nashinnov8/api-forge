services:
  api:
    build: .
    ports:
      - "7001:8080"
    depends_on:
      - db
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: {{ProjectName}}
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
